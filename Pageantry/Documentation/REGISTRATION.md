<!-- REGISTRATION DOCUMENTATION -->

Tools and Methods Include
JSON Web Tokens

🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴REGISTRATION VIEW🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴
        🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴BACKEND🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴
🔴🔴The user needs no special permission in order to access this view thus permission_classes = [AllowAny]
🔴🔴Again the user needs not to be authenticated before accessing this view thus authentication_classes = []

🔴🔴 This view accepts only post requests
🔴 Due to the inbuild registration checks in the django forms i decided to use CreateUserForm to handle user registration since it's more feasible and reinforces DRY
🔴 If the form is valid we do the following
    1. We save it 
    2. We send a prompt telling the user to login 

 If the form is not valid,
    1. We send the errors from the form to the user in the front end



RegisterView
🔴🔴🔴🔴Overview
🔴🔴The RegisterView handles POST requests to create new user accounts. It bypasses standard authentication and permission checks to ensure that unauthenticated users (new registrants) can access the endpoint.

🔴🔴Class Attributes
🔴permission_classes: Set to [AllowAny]. This allows any user, regardless of whether they are logged in or not, to access this view.

🔴authentication_classes: Set to an empty list []. This ensures that the view does not attempt to verify a user's token or session before processing the request.

🔴🔴🔴Methods
🔴🔴post(self, request)
Processes the incoming registration data.

 1. Data Initialization The view initializes a CreateUserForm ( which extended the UserCreationForm from django.forms) using the data sent in the request body (request.data).

2. Validation Logic The code checks if the form is valid using form.is_valid().

If Valid:

    The user is saved to the database via form.save().

    Returns a success response containing a custom status code and success messages.

If Invalid:

    The view skips the success block and generates an error context.

    The statusText field will contain the specific validation errors generated by the form (e.g., "This email is already in use").

🔴🔴Response Schema
Success Response (HTTP 200 OK)

🔴🔴JSON
🔴{
    "code": "registration_successful",
    "type": "success",
    "statusText": [
        "Account registration successful",
        "Proceed To Login"
    ]
}
Error Response (HTTP 200 OK)
Note: While the data indicates an error, the code currently returns a standard 200 response.

JSON

{
    "code": "invalid_form",
    "type": "error",
    "statusText": {
        "field_name": ["Error message"]
    }
}

🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴FRONTEND🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴🔴

🔴🔴SETUP
🔴For registration, only four input elements are used and they are strictly to collect the data [username, email, password1, password2]

🔴 Additional components include a link to the login page 


🔴🔴LOGIC
🔴 It is wrapped inside a parent element called Form which handles all the logic. 
🔴 Although there is a submit button in the registration component, the event listener is actually attached to the parent form component
🔴 There is no validation aside the REQUIRED TAG in html
🔴 All validation is handled by the CreateUserForm in the django backend 
🔴 If successful registration, user gets notified and gets sent to the login page 
🔴 If not, the error is displayed on the FORM 




THE PATHNAME FOR THIS VIEW IS api/auth/register